# Install as a package (recommended; adds CLI `inat-vision-saliency` on PATH):
#   pip install -e tools/inat_vision_saliency
#
# Or install the same dependencies without editable mode:
tensorflow>=2.12,<2.22
tf2onnx>=1.14
torch>=2.0
onnx>=1.14
onnx2torch>=1.5
numpy>=1.24
Pillow>=10.0
matplotlib>=3.7
