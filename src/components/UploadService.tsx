import useUploadObservationWorker from "components/MyObservations/hooks/useUploadObservationWorker";

const UploadService = ( ) => {
  useUploadObservationWorker( );
  return null;
};

export default UploadService;
