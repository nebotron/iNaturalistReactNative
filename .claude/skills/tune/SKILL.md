---
name: tune
description: Improve performance of the CV-based subject detector.
---

There is a script scripts/eval_onnx_variants.py for evaluating crop quality. Report the current evaluation of the subject detection algorithm on the entire set of labeled images from the Firebase crop log, which is >300 images. Try to improve it, and report the new score. Explore all available options, including different models, different model configurations, different heuristics, or retraining the model. If the new score is better, apply the subject detection changes to the React Native app. In parallel, also try to improve the algorithm for automatically picking brightness based on firebase training data. Commit your entire model into git. When you are done, leave notes of approaches that did and did not work at the end of this skill.

---

## Notes from tune sessions

### Setup

- Model: `ios/iNaturalistReactNative/yolov8n.onnx` — single-class YOLOv8n, output `[1, 5, 8400]`
- Post-processing: NMS (IoU=0.45, conf=0.05) → gate (0.25 → saliency fallback) → union top-K boxes → pad → square crop
- Eval: `python3 scripts/eval_onnx_variants.py crop_training.json`
- Dataset: `crop_training.json` = 605 Firebase images. Canonical; fetched from Firebase and committed as fallback.
- **Deps required:** `pip install numpy onnxruntime pillow opencv-python-headless` (and `ultralytics torch` to retrain). `evaluate_subject_detector.py` (imported by the eval script) was deleted in a past "Update skills" commit — restore it from git history if missing.
- **Eval gotcha:** if `cv2` (opencv) is NOT installed, the spectral-saliency fallback returns `None`, so every gated image (~25%) is *silently dropped* from the eval → inflated subset score. Always install opencv.
- **Image fetch (this env):** `static.inaturalist.org` and `firebaseio.com` are blocked by egress policy; the S3 mirror `inaturalist-open-data.s3.amazonaws.com` works. Originals rate-limit under high concurrency — use ≤8 workers, retries, and fall back to `medium`/`large` size (normalized boxes are size-independent since YOLO runs at 640).

### Session 2026-06-26: algorithm tuning (no improvement)

**Baseline: 0.918 on 328/402 images** (74 blocked at static.inaturalist.org, now resolved via S3 mirror)

Tried: algorithm variants (top1, sal_isect, smallest), 8 custom post-processing algorithms, 504-config parameter sweep, TTA (flip variants). All within ±0.001. GPU retraining not available (no CUDA; CPU 8–9 min/epoch). No changes deployed.

### Session 2026-06-27: MPS retraining

**Baseline: 0.700 on 605 Firebase images** (union+pad=0.08 optimal; deployed union+pad=0.03 ≈ same)

MPS (Apple Metal) works for training: ~25s/epoch vs 8–9 min CPU. `retrain_from_cache.py` fixed: `device="mps"`, removed `set_classes()` (YOLO World-only), export float32 ONNX.

### Session 2026-06-29: retrain yolov8n on user crop boxes — DEPLOYED ✅

**The win: retrain.** Fine-tuned `yolov8n.pt` directly on the 605 user crop boxes as single-class labels (515/90 train/val split, CPU ~100–120 s/epoch on 4 cores, 71 epochs w/ early-stop, ~2.3 h). Exported float32 ONNX (drop-in: identical `[1,3,640,640]→[1,5,8400]` signature) and replaced `ios/iNaturalistReactNative/yolov8n.onnx`.

Honest held-out (90-img val) comparison, **forced-YOLO** (no gate/fallback, to remove the saliency confound — see below): old **0.683 → new 0.747** (+0.064); recall 0.800→0.865, precision 0.447→0.512, zero-detection images 9→2. Full-pipeline (gate 0.25 + fallback): old 0.694 → **new 0.763**. The repro recipe is `retrain_from_cache.py` (default base now `yolov8n.pt`, was the incompatible/missing `yolov8s-worldv2.pt`).

**What did NOT work / dead ends:**
- *Post-processing & algorithm tuning* (gate/union-thresh/maxk/pad sweeps): only ±0.005–0.007, i.e. noise. Lowering `YOLO_UNION_THRESH` 0.6→0.4 helps the *old* model but not the retrained one; reverted. Best new-model post-proc (uthr 0.5/k 2/pad 0.08 = 0.768) is within 90-sample noise of the originals (uthr 0.6/pad 0.03 = 0.763) — left constants at defaults.
- *Gate tuning is a trap.* Raising the gate "improves" the eval because the eval's spectral-saliency fallback has recall ≈0.996 (huge boxes), so sending more images to fallback inflates the recall-weighted score. **The app's real fallback is Apple Vision attention saliency, not spectral** — unavailable on Linux — so any tweak that changes firing rate is non-transferable. Compare *models* with forced-YOLO (ignore gate+fallback) for an honest box-quality number.
- *Undertraining misleads.* At epoch ~29 the new model looked only ~tied (0.70) and even regressed at epoch ~40 (firing-rate confound); it only pulled clearly ahead after convergence (~epoch 51 best). Don't judge a CPU run before it finishes.
- *Full-605 eval of the retrained model reads 0.874 but is meaningless* (515/605 were training images → leakage). Use the held-out val numbers.

**Brightness: BLOCKED as of this session — could not tune.** The brightness labels live only in Firebase at `{CROP_LOG_FIREBASE_URL}/brightness_log.json`; `firebaseio.com` is denied by this environment's egress policy (proxy `connect_rejected`), and there is no committed fallback (unlike `crop_training.json`). Without the human-chosen brightness labels there is no ground truth to fit `TARGET_LUMINANCE`/the adjustment curve in `useAutoBrightnessForUri.ts`, so no validated change was possible. To make progress next time: export `brightness_log.json` to a committed file (mirror the crop-log fallback pattern) from an environment that can reach Firebase. **Update: unblocked 2026-07-04, see below** — `firebaseio.com` was reachable in that session's environment.

### Session 2026-07-04: refreshed dataset retrain (DEPLOYED ✅) + brightness model (DEPLOYED, low-confidence)

**Setup differences from prior sessions:** this environment could reach `firebaseio.com` directly (no S3-mirror workaround needed for the JSON endpoints, only for original image bytes), so both `crop_log.json` and `brightness_log.json` were pulled live. `scripts/pull_crop_log.py` merged Firebase into `crop_training.json`: 605 → **2502 images**. `scripts/prefetch_images.py` (new) downloads the whole set concurrently (8 workers) into `/tmp/inat_eval_cache` before running any eval/retrain — the serial per-image download inside `evaluate_subject_detector.resolve_image` is far too slow at this dataset size (2502 images), don't run the eval scripts cold.

**Subject detector — retrain on the refreshed 2502-image set, DEPLOYED.**
Baseline (previously-deployed model, full 2502-image set, deployed config pad=0.03/union): **0.798**. Best padding for that same model was only 0.800 at pad=0.10–0.12 — a ~0.002 gain, within the noise band established in the 2026-06-29 session; left padding at 0.03.

Retrained `yolov8n.pt` from scratch (not from the already-finetuned weights) on all 2502 images via `retrain_from_cache.py` (default args), 85/15 split (2126/376), MPS, batch=8, patience=20. **78 epochs completed in 5.6 hours, best at epoch 58.** Training is CPU/GPU-bound but wall-clock was inflated by repeated ~15-minute stalls that turned out to be **system sleep**, not slowness — `caffeinate -i` alone did not prevent it (probably clamshell/lid-close sleep), but `caffeinate -d -i -s -w <pid>` (also blocks display + system sleep) fixed it immediately. **Always launch long training runs under `caffeinate -d -i -s -w <pid>` from the start.**

Honest held-out comparison on the exact 376-image val split (`scripts/eval_heldout_val.py`, new — loads YOLO-format labels straight from `/tmp/inat_finetune_local/labels/val`, so it doesn't depend on the eval cache matching by URL), **forced-YOLO** (no gate/fallback):
old **0.806 → new 0.829** (pad swept 0.00–0.30 for both); recall 0.900→0.914, precision 0.431→0.487, no-detection images 56/376→26/376. Clearly above noise. Model installed to `ios/iNaturalistReactNative/yolov8n.onnx` and committed.

**Brightness — unblocked, but data is small and biased. Shipped a model anyway at the user's request.**

`brightness_log.json` had only **21** human labels (`{url, brightness}` — the ideal multiplier a user picked with `MainMediaDisplay`'s manual slider before hitting save; independent of any auto-suggestion, since that screen starts the slider at a hardcoded 1.0 regardless of `useAutoBrightnessForUri`). Committed as `brightness_log_raw.json` (mirrors the `crop_training.json` fallback pattern).

*First pass — full-image luminance (matches what the human actually looked at in the slider UI):* correlation between mean luminance and the chosen multiplier was **positive** (r=0.46) — the opposite sign from the deployed formula's assumption (`TARGET_LUMINANCE/luminance`, which assumes darker → brighten more). Two images with nearly identical luminance (0.271) got opposite human choices (0.79× and 1.87×). A from-scratch LOOCV search (`scripts/explore_brightness_models.py`) over ~20 candidate models (linear/log-linear/multivariate regression on mean/geomean/median/std/percentiles/shadow-highlight fractions, highlight-protection exposure metering, k-NN) found **nothing beat a naive constant-mean prediction** (LOOCV MAE 0.673 constant vs 0.641 best content-model) — recommended *not* shipping, since the 21 labels are a biased sample (only logged when a user felt the auto-brightness was wrong, mostly under-exposed) and a model fit to that sample would over-brighten already-correct photos.

*User overrode: "ship it, I will label more data based on the errors."* Also asked to make sure the algorithm runs on the **detected subject crop**, not the full frame (this is what `ObsImage.tsx`'s `useAutoBrightnessForUri` call already does in production — `brightnessCrop` is the subject-detection box). Recomputed the same feature/LOOCV search over crop-region pixels instead of full-image (`scripts/explore_brightness_crop_models.py`): correlations flipped to the theoretically sensible sign (crop `p10`, i.e. shadow depth, correlates **negatively** with the chosen multiplier, r=−0.475 — darker crop shadows → more brightening), and `loglinreg(p10)` won clearly: **LOOCV MAE 0.573 vs 0.641 constant** (~11% better), the best of every candidate tried on either full-image or crop features.

Deployed: `adjustment = exp(-1.463 × shadowP10 + 0.804)`, clamped to `[0.4, 3.0]` (same safety bounds as before), where `shadowP10` is the 10th-percentile luminance of the subject crop. Required a native change since the old `measureAverageLuminance` only computed a mean: renamed to `measureShadowPercentile` in `ImageCropper.m`, same 64×64 sampling geometry (crop-parameterized already), now `qsort`s the 4096 samples and returns the 10th-percentile value. `useAutoBrightnessForUri.ts`'s `computeAdjustment` replaced with the log-linear formula (constants `SHADOW_ADJUSTMENT_SLOPE`/`INTERCEPT`).

**Caveat for next session:** n=21 is small and this is a first cut, not a validated win — LOOCV MAE improvement over baseline is real but modest, and there was no held-out test beyond LOOCV on the same 21 points. If more labels come in, refit by rerunning `scripts/explore_brightness_crop_models.py` against `brightness_log_raw.json` (or refresh it from Firebase first) and update the two constants in `useAutoBrightnessForUri.ts`.

### Session 2026-07-05: brightness-only re-check (no formula change) + auto-brightness toggle — DEPLOYED ✅

**Scoped to brightness only this session** (no subject-detector work). `firebaseio.com` was blocked again in this environment (`Tunnel connection failed: 403`) — same intermittent-access pattern as prior sessions — so no new brightness labels beyond the 21 in `brightness_log_raw.json` were obtainable. `scripts/analyze_brightness.py`'s cached feature file (`/tmp/brightness_crop_features.json`) doesn't survive between sessions and the script that built it was never committed, so **added `scripts/compute_brightness_crop_features.py`** (committed) — it re-derives crop-region luminance features for `brightness_log_raw.json` by running the *actual deployed* ONNX model + `algo_union` (imported from `eval_onnx_variants.py`), not the generic ultralytics `yolov8n.pt` COCO detector that `evaluate_subject_detector.detect_bounds_yolo_current` loads (that one requires downloading pretrained weights from GitHub, which was also blocked, and isn't what's deployed anyway).

Re-ran `scripts/explore_brightness_crop_models.py` on the freshly-derived features. Current deployed model (`loglinreg(p10)`) scored **LOOCV MAE 0.580** vs constant baseline **0.641** — consistent with the 0.573 documented previously (small delta is re-download/box jitter noise). Two candidates scored nominally *better*: plain `linreg(p10)` (0.552) and `kNN(k=5, all feats)` (0.494). **Neither was deployed:**
- `linreg(p10)` (no `exp()`) fits the 21 biased (mostly-underexposed) samples slightly better but extrapolates badly outside their p10 range: at a mid-brightness crop (p10≈0.5, a well-exposed photo *not* represented in the biased label sample) it predicts adjustment≈0.35, clamped to the 0.4 floor — i.e. it would incorrectly darken correctly-exposed photos. The deployed log-linear form predicts ≈1.0 at the same point, which is the sane default absent evidence either way. Lower LOOCV MAE here is an artifact of the label sample's skew, not a real improvement.
- `kNN(k=5)` would require bundling all 21 raw feature/label pairs into native code and doing on-device nearest-neighbor search — real complexity increase for an unvalidated gain on 21 points, and it's simply memorizing/averaging rather than learning an extrapolatable rule.

**Net: brightness formula unchanged.** Re-run this again once more labels land (rerun `scripts/compute_brightness_crop_features.py` then `scripts/explore_brightness_crop_models.py`); don't chase LOOCV MAE improvements on this dataset without also sanity-checking predicted adjustment across the *full* p10 range, since the label sample itself is skewed toward underexposed photos.

**Added:** a user-facing "Auto Adjust Brightness" toggle in Settings → Advanced Settings (`autoAdjustBrightness` in `createLayoutSlice.ts`'s `layout` namespace, default `true`, persisted via the existing zustand/MMKV store — same pattern as `isAllAddObsOptionsMode`). Wired into `ObsGridItem.js`, the only call site that ever sets `autoAdjustBrightness={true}` on `ObsImagePreview`/`ObsImage`.

### Session 2026-07-07: brightness refit on 73 labels (3.5x more data) — DEPLOYED ✅

**Scoped to brightness only** (`ARGUMENTS: just for brightness`), no subject-detector work. `firebaseio.com` was reachable this session. `brightness_log.json` had grown from 21 → **73** valid labels (all 21 old entries present as a subset). Overwrote `brightness_log_raw.json` with the fresh 73-entry fetch.

Ran the existing pipeline unmodified: `scripts/compute_brightness_crop_features.py` (all 73/73 images resolved a crop via the deployed model) → `scripts/explore_brightness_crop_models.py`. `loglinreg(p10)` (the deployed model family) remained the best *defensible* choice — `loglinreg(frac_dark)` scored marginally better (0.5240 vs 0.5311 LOOCV MAE) but the gap is within noise for n=73 and would require new native code (frac-of-dark-pixels) vs. reusing the already-wired `shadowP10`/`measureShadowPercentile`. kNN(k=5) again "won" (0.4220) but was rejected again for the same reason as 2026-07-05 (memorization, not an extrapolatable rule, needs bundling raw points on-device).

**The refit itself is the real win this session, independent of feature choice:** the *currently-deployed* coefficients (fit on the old, small, biased 21-sample set) score only **0.612 MAE** when re-scored against the new 73-sample set — barely beating the naive constant-mean baseline (0.639). A fresh `loglinreg(p10)` fit on all 73 points scores **0.531 LOOCV MAE** (~13% better than deployed-on-new-data, ~17% better than baseline). The predicted-adjustment curve also now crosses neutral (1.0×) at a more sensible p10≈0.40 (vs the old fit's p10≈0.55), and stays sane across the full observed range (p10=0 → 1.82×, p10=1 → 0.40× clamp floor) — see the curve dumped in-session for the full table.

Deployed: recalibrated `SHADOW_ADJUSTMENT_SLOPE`/`INTERCEPT` in `useAutoBrightnessForUri.ts` from `(-1.463, 0.804)` → `(-1.510521, 0.597565)`. No native code changes — same `shadowP10` feature/native measurement as 2026-07-04.

**Lesson for next time:** the 21→73 label growth alone (no algorithm change) was enough to meaningfully invalidate the previous fit's calibration. When labels roughly triple, re-fitting the *same* feature is worth doing before hunting for a better feature — don't assume last session's "which feature wins" conclusion needs to be revisited, but do assume the *coefficients* are stale. Keep re-running `compute_brightness_crop_features.py` → `explore_brightness_crop_models.py` each session labels grow; refit-on-more-data is cheap and the win here was bigger than any feature-search win in prior sessions.

### Session 2026-07-08: dataset refresh to 3433 entries, retrain — DEPLOYED ✅ (subject detector); brightness unchanged (1 new label)

**Subject detector.** `scripts/pull_crop_log.py` merged Firebase into `crop_training.json`: 2502 → **3433 images**. `scripts/prefetch_images.py` cached 3143/3433 (91.5%; the rest 404/blocked-host misses, same as always).

Baseline (previously-deployed model — the 2026-07-04 retrain — full 3433-entry set, deployed config pad=0.03/union): **0.866** (recall=0.950, prec=0.531). This *includes* training-set leakage from the prior session's fit, so it's optimistic; not used for the go/no-go decision.

Retrained `yolov8n.pt` from scratch on the full refreshed 3433-entry set via `retrain_from_cache.py` (default args), 85/15 split (2671/472), MPS, batch=8, patience=20, launched under `caffeinate -d -i -s -w <pid>` from the start (no sleep stalls this session — training ran end-to-end in ~6.5 hours real time with no unexplained gaps, vs the 2026-07-04 session's repeated 15-min sleep stalls). Ran the full 100 epochs (never hit the patience=20 early-stop; mAP50 was still creeping up at epoch 95, best checkpoint effectively the final one).

Honest held-out comparison on the exact 472-image val split (`scripts/eval_heldout_val.py`), **forced-YOLO** (no gate/fallback, avoids the saliency-fallback confound — see 2026-06-29 notes above): old **0.832 → new 0.842** (pad swept 0.00–0.30 for both); recall 0.916→0.924 at old's best pad, precision 0.497→0.510 at the same point — both metrics improved, not just a pad-sweep artifact. Delta +0.0105, consistent with the ~0.01+ "not noise" bar established in past sessions (smaller than the 2026-07-04 session's +0.023, as expected — each successive retrain on incrementally-more data yields diminishing returns). New model's best pad (0.05, score 0.842) is statistically indistinguishable from its score at the deployed pad=0.03 (also 0.842) — left `SUBJECT_DETECTION_MODEL_PADDING` unchanged at 0.03 in `src/sharedHelpers/subjectDetectionModels.ts`.

Model installed to `ios/iNaturalistReactNative/yolov8n.onnx` (done automatically by `retrain_from_cache.py`) and committed.

**Brightness.** `brightness_log.json` had grown only 73 → **74** (one new label) since the 2026-07-07 session — far too little to justify a refit. Confirmed via the established sanity check: the currently-deployed coefficients `(-1.510521, 0.597565)` scored **0.528 MAE** against the fresh 74-sample feature set (`compute_brightness_crop_features.py` → manual scoring), vs a from-scratch refit's LOOCV MAE of 0.541 — deployed is already at least as good as a fresh fit would be. No change made. Re-check next session once more labels accumulate (the 2026-07-07 lesson holds: a ~3x label increase is worth a refit, a +1 is not).

**Process note:** background `Agent`/bash training runs that take multiple hours will straddle *other* commits landing on `main` from unrelated work (a photo-picker feature and several bug fixes landed on `main` during this session's ~7-hour training run, including an automated "Update training logs" commit from a concurrent process that had already pulled the same Firebase data). Before committing at the end of a long run: `git fetch fork && git rev-list --left-right --count fork/main...HEAD` to check for drift, diff the specific files you're about to commit against the remote tip to confirm no conflict, then rebase before pushing rather than assuming HEAD hasn't moved.

### Session 2026-07-13: dataset to 6300, retrain DEPLOYED ✅; brightness switched to geomean+median — DEPLOYED ✅

**Dataset.** `pull_crop_log.py`: 3433 → **6300 entries**, but 1893 are `file://` URIs (in-app photos never uploaded — only exist on the user's device, silently skipped by `resolve_image` with no log line). Usable: **4407** (was 3143). When prefetch "fails" a big chunk, check the `file://` count before suspecting the network: `python3 -c "…url.startswith('file://')…"`.

**Subject detector — retrained, deployed.** Baseline (previous model, full evaluable set, union/pad=0.03): **0.875**. Retrained from `yolov8n.pt` on 4407 images (3745/662 split, 100 epochs, MPS ~3.5 min/epoch ≈ 7 h total).

**The leakage trap got worse this session — naive held-out val is now WRONG.** `eval_heldout_val.py`'s val split is held-out only for the *new* model; ~73% of those images were in the *old* model's training set, so the naive comparison read old 0.872 vs new 0.861 — the old model was winning on its own memorized training images. Filtering the val split to the **178 images neither model ever saw** (map val's `00123.jpg` names back to cache photo-IDs by file-size+md5 — `retrain_from_cache.py` writes no manifest; old training photo-IDs from `git show <old-rev>:crop_training.json`) flipped it decisively: **old 0.820 → new 0.876** (+0.056; recall 0.909→0.955, prec 0.467→0.563). Deployed. Best pad still 0.03. Full-set score of the new model: 0.900 (leakage-inflated headline; the 178-image number is the honest one). **Next session: compare on val ∖ old-training-set, not raw val** — each successive retrain makes the naive comparison more biased toward "old wins". Consider making `retrain_from_cache.py` write a `manifest.json` (index → source URL) to skip the hashing dance.

**Long-run survival (this env kills tasks).** The harness killed the first training run at ~epoch 14 and even a `nohup`-detached resume died silently at ~epoch 80 (no traceback; jetsam/memory-pressure suspected — the machine did not reboot). Ultralytics checkpoints every epoch: resume with `YOLO(last.pt).train(resume=True)` (then export/install manually, mirroring the tail of `retrain_from_cache.py` — see scratchpad `resume_train.py` pattern). **Launch training detached (`nohup … & disown`) from the start, monitor `/tmp/inat_train_local/run/results.csv` (the process's stdout pipe buffers everything if you pipe through `tail`), and expect to resume at least once.**

**Brightness — feature change, deployed.** Labels 74 → **100**; the 26 new labels are more extreme (max 5.36×, 17 labels >3.0×; constant-baseline LOOCV MAE jumped 0.64 → 0.97). Deployed p10 coefs re-scored 0.756 MAE on the new set, and a same-feature refit was a statistical no-op (0.754) — but a 2-feature search won honestly: **`loglin(geomean, median)` LOOCV MAE 0.683**, paired-bootstrap P(better than deployed)=0.98. The opposite-signed coefs (−4.167·geomean + 2.612·median + 0.828) measure shadow-tail skew: geomean far below median = backlit subject → brighten hard. Extrapolation sanity-checked (≈1.0× at mid-bright crops; the impossible corner geomean>median can't occur since geomean≤median for these distributions in practice). Native `measureShadowPercentile` → `measureCropLuminance` returning `{geomean, median}` (same 64×64 sampling; geomean clips at 1e-4 pre-log to match `compute_brightness_crop_features.py` — keep Python/native definitions in sync). Clamp stays [0.4, 3.0]: raising the ceiling changes nothing because the fit never predicts >2.3 (high-label/low-p10 images have high variance; a single formula can't reach the 5× labels). kNN "won" again (0.70) and was rejected again (memorization, on-device complexity).

**Concurrent-session hazard (new, worse than remote drift).** Another Claude session was committing *in this same working tree* during the session: its "Running /tune" commit swept up this session's uncommitted edits (dataset + brightness changes) mid-flight, and unrelated feature commits (auto-brightness Off/Multiply/Gamma switch on the Identify page) landed locally. Content survived, but commit structure didn't; don't rewrite history to fix it (the other session may build on those SHAs — and never force push anyway). Check `ps aux | grep retrain` before launching training to avoid a duplicate run; verify formula changes still compose with concurrently-landed consumers (they did: `useAutoBrightnessForUri` returns a multiplier, callers don't care how it's computed). Also: a stray pre-existing unused-i18n-key (`View-observation`) blocked ALL commits via the pre-commit hook — fix such things in a separate tiny commit first.

### Session 2026-07-15: brightness head baked into the detector ONNX — DEPLOYED ✅ (model-based brightness, replaces crop-stats formula as primary)

**Scope:** user asked to make the *subject detector itself* output brightness instead of the stats heuristics. Labels 100 → **193** (`pull_logs.py` now writes `brightness_training.json`, which superseded `brightness_log_raw.json` as canonical — the compute/extract scripts were repointed to it). Crop log 6300 → 6691 (committed, but no detector retrain this session).

**What shipped:** a ridge-regression brightness head baked directly into `yolov8n.onnx` as a second graph output `brightness` [1,1] (already exp'd and clamped [0.4, 3.0] in-graph). Head inputs, all tensors already in the graph: **SPPF channel means (256, `GlobalAveragePool` of `/model.9/cv2/act/Mul_output_0`) + 12 photometric stats** of the letterboxed input's 80×80-pooled luminance (global + objectness-weighted mean/log-mean/second-moment per P3/P4/P5 scale, weights = `Sigmoid` of the Detect head's cls logits). `scripts/bake_brightness_head.py` refits from `/tmp/brightness_yolo_features2.npz` (built by `scripts/extract_yolo_brightness_features2.py`) and verifies before overwriting: `output0` must be bit-identical (it was, 0.00e+00 — the head only reads existing tensors) and ONNX-vs-Python head parity <5e-3 (2.3e-4). Native `detectSubjectBounds` returns `brightness` alongside the bounds dict (also on the saliency-fallback path — YOLO always ran; gated only by a `SessionGetOutputCount>=2` check so an old model file still works); it rides on `NormalizedCrop.brightness` through `detectSubjectInImage` → `useAutoBrightnessForUri`, which prefers it and keeps the geomean/median stats formula as fallback (manual/logged crops, full-image measurement).

**Honest numbers on the 193 labels (all clamped [0.4,3.0], grouped LOOCV):** deployed stats formula 0.665 MAE; refit loglin(geomean,median) 0.656; refit loglin(all 10 crop stats) 0.601; **model head 0.568** (paired bootstrap P(better than deployed)=1.000). Ridge alpha=100; alpha sweep was flat 70–150.

**What did NOT work / lessons:**
- *Plain mean-pooled deep features are NOT enough:* best pure-backbone ridge (SPPF, alpha=100) = 0.668 ≈ tie with the stats formula. The win came from *combining* SPPF means with the 12 in-graph photometric stats (0.568). Photometric stats alone = 0.606. Deep features and luminance stats are complementary.
- *Exposure augmentation HURT accuracy everywhere* (e.g. sppf+photo 0.568 → 0.675 with 7 synthetic exposure variants per image at label/f). Suspect the synthetic exposure distribution mismatches real capture conditions. But the no-aug model *under-reacts* to synthetic exposure shifts (predicts 1.79 where ideal is 2.65 at f=0.4) — direction correct and monotone, magnitude conservative. If labels keep growing and under-brightening complaints appear, revisit low-weight augmentation.
- *Objectness-weighted pooling of deep features* (wpool448) was the worst block tried (0.75–1.0) — weighting *feature maps* by detection confidence destroys signal, even though the same weighting applied to *luminance stats* helps.
- *kNN not retried; MLP skipped* (no sklearn in env; ridge win was already large and bake-friendly).
- *Numpy on this Mac spews spurious `RuntimeWarning: overflow in matmul` from Accelerate* — results were finite and parity-verified; don't chase it.
- *Concurrent-session hazard again:* another session's "Trying to get brightness from subject detector" commit swept up this session's in-progress scripts mid-flight (content survived; didn't rewrite history). Also two pre-existing pre-commit blockers (a Flow error in `LocationPickerContainer`, unused i18n key `Zoom-in-as-much-as-possible-to-improve`) had to be fixed in a separate tiny commit first — same pattern as 2026-07-13.

**Next time:** the head is cheap to refit as labels grow — rerun `extract_yolo_brightness_features2.py` (only needs the f=1.0 pass unless re-testing augmentation) then `bake_brightness_head.py`; both verify parity automatically. If the *detector* is retrained, the head MUST be re-extracted and re-baked on the new backbone (features change). `eval_onnx_variants.py`'s `sess.run(None,...)[0]` still picks output0 with the 2-output model, so all crop evals work unchanged.

### Session 2026-07-16/17: Firebase locked down — reads need creds, app is write-only

Database rules changed: **reads require auth, writes stay open**. The app carries NO credentials by design (user directive) — its log sync is write-only, PUT/DELETE of single url-keyed children at `crop_log/<encoded-url>` and `brightness_log/<encoded-url>` (keys = `encodeURIComponent(url)` with `.`→`%2E`; values carry the url). The remote nodes are therefore *objects*, not arrays; all pull scripts (`pull_logs.py`, `pull_crop_log.py`, `evaluate_subject_detector._fetch_firebase`) handle both formats. The in-app CropLogViewer shows the device-local log only.

Scripts authenticate via `scripts/firebase_auth.py`: set in `.env` either `FIREBASE_DB_SECRET` (legacy database secret, used directly as `?auth=`) or `FIREBASE_API_KEY` + `FIREBASE_EMAIL` + `FIREBASE_PASSWORD` (Identity Toolkit email/password sign-in). **As of 2026-07-17 the Web API key was still missing from .env** (email/password alone can't sign in — Google's REST API requires the project's Web API key; it's not in the repo, all GoogleService-Info.plists are placeholders, project id `inaturalist-9001d`). If pulls 401, that's why.

Also fixed en route: the app's old read-merge-PUT sync would have clobbered the remote log with local-only data whenever a read failed; per-entry writes eliminate that class of bug entirely.

### Session 2026-07-20: brightness head refit on 237 labels — DEPLOYED ✅; detector retrain BLOCKED (no base weights on Linux)

**Environment:** Linux, **4 CPUs, no GPU/CUDA**. `firebaseio.com` reachable via `FIREBASE_DB_SECRET` (user supplied creds mid-session; put them in `.env`, which is gitignored via `.env*`). Deps not preinstalled — `pip install numpy onnxruntime pillow opencv-python-headless onnx` and `pip install torch --index-url https://download.pytorch.org/whl/cpu` + `pip install ultralytics` (the pytorch CPU index does NOT carry ultralytics; install them from separate indexes).

**Detector — reported baseline, no change (retrain not possible here).**
`pull_logs.py`: crop 6691 → **7095** (2177 `file://`, 4918 usable; 4899 actually cached). Deployed model on the full 4899-image evaluable set (union, pad=0.03): **score 0.8945** (recall 0.975, prec 0.572); pad=0.00 read 0.8972 but that's within the documented ±0.005–0.007 noise band and on training-leaked data — left pad at 0.03. Ran a lean single-pass eval (`/tmp/lean_eval.py` pattern — resolve *cached-only*, run detection once, sweep pad on union) instead of `eval_onnx_variants.py`, whose 4-algo re-detection + serial cold-resolve is far too slow at this dataset size.
- **Retrain is BLOCKED in this environment, two independent ways:** (1) no GPU, and CPU at ~15–28 min/epoch × 60+ epochs to converge = 15–45 h, which the kill-prone ephemeral container cannot survive; (2) more fundamentally, `retrain_from_cache.py` needs the COCO base `yolov8n.pt`, which ultralytics fetches from `github.com/ultralytics/assets/releases/...` — **egress policy 403s that host** (curl confirms 403 on every version tag; proxy README says do not route around policy denials). No cached `.pt` exists anywhere on disk, and the deployed model is onnx-only (can't resume training from it). **All prior successful retrains ran on the user's Mac (MPS + github reachable); they are not reproducible in the Linux web-session env.** Next time a retrain is wanted, either run it where github assets are reachable, or first commit a `yolov8n.pt` into the repo as a fallback base (mirror the `crop_training.json` pattern) so ultralytics can load it locally.

**Brightness — head refit on 237 labels, DEPLOYED.** `brightness_training.json`: 193 → **238** (237 usable; 1 junk test entry, photo id `123456789`). Reran `extract_yolo_brightness_features2.py` (f=1.0 only is enough — bake and the deployed config both use `factors==1.0`; a 1-line `EXPOSURE_FACTORS=[1.0]` copy cuts the work 7× and dodges the mid-run kill that the full 7-exposure pass hit under CPU contention). `train_brightness_head2.py` reconfirmed **sppf+photo / aug=False / alpha=100** is still the best block (LOOCV MAE 0.5379 over all 237; the tail is dominated by new extreme labels — range now to 9.6×, which the [0.4,3.0] clamp can't reach). Honest held-out check on the **44 genuinely-new labels** (not in the prior 193-label fit; get them via `git show <prev-rev>:brightness_training.json`): deployed head **0.4381 → fresh refit 0.4207** — refit generalizes better, so re-baked. Re-baked head on all 237: 0.4195 (was 0.4367).
- **Re-bake gotcha (new):** `bake_brightness_head.py` assumes a *head-less* detector and dies with `bh_lum_w initializer name is not unique` when run on the already-baked deployed onnx. Fix: strip the old head first (drop nodes named `/bhead*`, initializers `bh_*`, and the `brightness` output → verified bit-identical `output0`), bake fresh onto that. Left the strip as an inline `/tmp` script; if this recurs often, fold a "strip existing head if present" guard into `bake_brightness_head.py`.
- Left the **fallback** geomean/median formula in `useAutoBrightnessForUri.ts` unchanged: it only fires when `crop.brightness` is null (manual/logged crops, old model files), which never happens for auto-brightness with the current 2-output model — refitting it has ~zero production impact.
- Also hardened `evaluate_subject_detector.resolve_image` to skip junk entries whose sanitized cache filename exceeds 200 chars (a bogus all-`a` photo id crashed prefetch/retrain/eval with "File name too long").

### Session 2026-07-21: blocked env (Linux, no GPU, no Firebase creds) — baseline confirmed, NO CHANGE

**Environment:** Linux, **4 CPUs, no GPU/CUDA**, and **no `.env` / no Firebase creds this session** — Firebase reads now require auth (401 without a secret), so `crop_log.json` / `brightness_log.json` were unreachable. Fell back to the committed datasets, which are **unchanged since 2026-07-20**: `crop_training.json` = 7095 entries (4108 S3 + 800 blocked-static + 2177 `file://` + 10 other → **4899 usable/cached**), `brightness_training.json` = 238 (237 usable). Deps: `pip install numpy onnxruntime pillow opencv-python-headless onnx` (torch/ultralytics not installed — not needed, retrain blocked). S3 mirror `inaturalist-open-data.s3.amazonaws.com` reachable; `static.inaturalist.org` 403.

**Subject detector — baseline reported, NO CHANGE (retrain infeasible, post-proc win is a leakage artifact).**
Deployed model on the full 4899-image evaluable set (union, uthr=0.60/maxk=3/pad=0.03): **score 0.8944** (R=0.975, P=0.572) — matches the 2026-07-20 reading (0.8945). Ran a lean single-pass sweep (`/tmp/lean_eval.py`: detect once per cached image, then sweep pad × union_thresh × max_k in-memory; `PIL.Image.draft("RGB",(640,640))` for fast JPEG decode makes the whole 4899-image pass ~5 min). Full sweep best was **0.8983 at uthr=0.40/maxk=3/pad=0.00** (+0.0039) — but this is the **documented leakage trap**: the deployed model trained on ~all 4899 images, so recall is saturated (0.97–0.98) and pad=0.00 "wins" only because extra padding can't buy already-maxed recall and only costs precision. On genuinely held-out images (every prior session's honest val sweep) the optimal pad is 0.03–0.05, so pad=0.00 would *regress* real-world usage. Dataset git history is squashed (single commit), so the held-out subset couldn't be reconstructed to prove it directly — but the reasoning is identical to 5 prior sessions. **Left detector + config unchanged.**
- **Retrain still blocked, but the base-weights blocker is now liftable:** confirmed again no GPU + CPU 15–45 h + kill-prone container = full retrain not survivable here. HOWEVER, **HuggingFace serves the COCO base weights** where GitHub hard-403s: `curl -L https://huggingface.co/Ultralytics/YOLOv8/resolve/main/yolov8n.pt` returns 302→206 with a valid `.pt` zip (GitHub `ultralytics/assets` 403s every tag). So a future Linux session *could* get `yolov8n.pt` (point ultralytics at the HF-downloaded file, or commit it as the fallback base the 2026-07-20 note asked for) — the only remaining detector-retrain blocker in this env is then pure CPU wall-time, which still isn't feasible without a GPU. Real retrains still belong on the user's Mac (MPS).

**Brightness — NO CHANGE (no new labels since 2026-07-20).** `brightness_training.json` still 238 (237 usable), byte-identical to when the deployed head was last baked. Re-ran the actionable CPU-only pipeline anyway to confirm optimality: `extract_yolo_brightness_features2.py` (EXPOSURE_FACTORS=[1.0]) → `train_brightness_head2.py` reproduced **sppf+photo / aug=False / alpha=100 → LOOCV MAE 0.5379** exactly (matches the deployed fit). Since data + code are unchanged, a re-bake yields identical weights — **left the head as-is.** Refit is worth re-running only once new labels land (Firebase reachable + creds).

**Net:** no model or code changes — both improvement levers are environment-blocked (detector retrain needs a GPU; brightness needs new labels that require Firebase creds this session lacked). Baseline is healthy and confirmed. Only committed change is these notes. **Lesson: a session with no GPU AND no new Firebase data cannot improve either model — recognize this early (check `.env`/Firebase auth + dataset counts vs. last session in the first few steps) and spend the session confirming the baseline rather than chasing leakage-inflated post-processing deltas.**
